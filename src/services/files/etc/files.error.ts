export enum FilesError {
  UnsupportedPayload = 'UNSUPPORTED_PAYLOAD',
  InvalidPayload = 'INVALID_PAYLOAD',
  InvalidUploadToken = 'INVALID_UPLOAD_TOKEN',
  MediaNotPresented = 'MEDIA_NOT_PRESENTED',
  ImageUploadError = 'IMAGE_UPLOAD_ERROR',
}
